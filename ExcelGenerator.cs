using System.IO;
using OfficeOpenXml;

namespace CryptoTransactionTracker
{
    public class ExcelGenerator
    {
        // used to generate an Excel file of the transactions that occurred on a wallet address that the user inserted into the application
    }
}
